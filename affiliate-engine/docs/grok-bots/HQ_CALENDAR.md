# HQ 円カレンダー（2026-09-11 リスケ → 席1回）

目標と期限は変えない。期間 2026-08-26〜2026-09-30。目標 確定 ¥1,000,000。
実測円は `conversions.csv` の `approved_yen`。無いあいだは ¥0。カタログ・CW・note は円ではない。

このファイルは **Cursor と人間の盤面**。Grok Bot は開けるな。入口は `dump/G_hq_boot.txt` と Issue `Grok Bot — 指示` の `hq-instruct:` だけ。Threads cron は戻すな。アフィ URL は書くな。

## 欠けていたもの

元の仕組みにあった期限は README の **9/30 月次確定 ¥30,000**（自動投稿前提）だった。
月100万はその上に載ったが、**日次ゲートが無かった**。自動投稿は 2026-08-26 から停止のまま。
1語 dump は円の手前で日を食った。GO 待ちも同じ。采配は **席1回** に畳み、dump は overlay と確定円で自動選択する。

## いまの実測（2026-09-11 JST）

| 項目 | 値 |
|---|---|
| 残日 | 20（9/11〜9/30 含む） |
| `approved-yen` | 0 |
| `overlay-filled` | 0 |
| 指示役 dump | `G_hq_human_sitting.txt`（GO 不要） |
| 人間の未完了 | A8 の席1回（教育プロフィールを先） |
| 停止のまま | Threads 自動投稿 / insight / 日次レポート |

## 日次の進捗確認（時計の本体）

Cursor を常時動かさない。確認は既にある指示役で足りる。

| いつ | 誰 | 見ること |
|---|---|---|
| 毎日 07:22 JST | Actions → Issue 110 | `hq-instruct:` / `hq-gate:` / `overlay-filled:` / `approved-yen:` |
| 人間が A8 を開いた日 | ナオミチ | 見えた確定だけ Issue 109 に `A8_YEN:` |
| Cursor が起こされた日 | Cursor | この表と Issue を照合し `output/sprint/TODAY.md` を1回更新して止まる |

プランニングの正は **今日のゲート**。指示役が overlay / 円で dump を選ぶ。1語は Cursor も Bot も要求しない。

## ゲート（JST）

円に直結する順。未達なら翌日も同じゲート。先の案件を足すな。1日1語で往復するな。

| 日付 | ゲート | 達成 | 未達の翌日 |
|---|---|---|---|
| **9/11–9/12** | 席1回 | 教育 Threads に N高またはアイズを置いた、または提携申請まで終わった。Issue 110 の `完了` は印。overlay が空なら dump は同じ | 同じ席。1語を返すな。FANZA・CW・XM に逃げるな |
| **9/13** | overlay | `overlay-filled` ≥ 1（鍵名だけ。URL は Issue に出すな） | Secret の既存 JSON に置いた鍵だけ足す。`links.json` は空のまま。転職アカが無ければ neo は置かない |
| **9/14–9/19** | 計測 | A8 画面を開いて `A8_YEN:`（見えた 0 は 0） | 開いていない日は行を足すな。プロフィールのリンクを外すな |
| **9/20** | 中間 | overlay の鍵数と `approved-yen` を記録。入口がまだ 0 なら **入口不足** と書く（円を invent するな） | 入口が 0 なら 21 日以降も席（置ける面）だけ。案件を増やさない |
| **9/21–9/27** | 既存面だけ | 既に置いたプロフィール／許可媒体。毎日 A8 を見た日だけ `A8_YEN:` | 自動投稿を戻すな。DMM アダルトを本線に足すな |
| **9/28–9/30** | 凍結計測 | 新規 dump 案件を足すな。見た円だけ | 期限を伸ばすな。目標を下げるな |

## 円の本線（これ以外はスケジュールに入れない）

1. 高単価を提携する（同じ席で N高 → アイズ → neo → チケット）
2. 人が踏める場所に広告リンクを置く（**教育プロフィールが先**。転職 Threads が無ければ neo / チケットは置かない。自動投稿は停止のまま）
3. 同じ URL を Secret に足す（Git に書くな。追加であり置換ではない）
4. A8 の確定を見たときだけ `A8_YEN:`

入れない: CW 応募数、カタログ 15000、XM ペーパー、H3、FANZA サイト構築、自動 DM、いいね自動化、1語チェーン、GO 待ち。

## Cursor が起こされた日の手順

1. Issue 110 の最新 `hq-instruct:` / `hq-gate:` / `overlay-filled:` / `approved-yen:` を読む
2. Issue 109 の `A8_YEN:` を読む（無ければ円は 0 のまま）
3. 上の表で **今日のゲート** が達成かを書く
4. `output/sprint/TODAY.md` を日付ごと更新する
5. 仕組みの穴だけなら `参謀へ:` 1行。ログイン確認で起き続けるな
