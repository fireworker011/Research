# HQ 円カレンダー（2026-09-11 リスケ）

目標と期限は変えない。期間 2026-08-26〜2026-09-30。目標 確定 ¥1,000,000。
実測円は `conversions.csv` の `approved_yen`。無いあいだは ¥0。カタログ・CW・note は円ではない。

このファイルは **Cursor と人間の盤面**。Grok Bot は開けるな。入口は `dump/G_hq_boot.txt` と Issue `Grok Bot — 指示` の `hq-instruct:` だけ。Threads cron は戻すな。アフィ URL は書くな。

## 欠けていたもの

元の仕組みにあった期限は README の **9/30 月次確定 ¥30,000**（自動投稿前提）だった。
月100万はその上に載ったが、**日次ゲートが無かった**。自動投稿は 2026-08-26 から停止のまま。
dump は1手ずつ進むが、「何日までに踏めるリンクがあるか」が無かったので、円の手前の確認が日を食った。

## いまの実測（2026-09-11 JST）

| 項目 | 値 |
|---|---|
| 残日 | 20（9/11〜9/30 含む） |
| `approved-yen` | 0 |
| `overlay-filled` | 0 |
| 指示役 dump | `G_hq_sns_next.txt` |
| 人間の未完了 | neo 1語 |
| 停止のまま | Threads 自動投稿 / insight / 日次レポート |

## 日次の進捗確認（時計の本体）

Cursor を常時動かさない。確認は既にある指示役で足りる。

| いつ | 誰 | 見ること |
|---|---|---|
| 毎日 07:22 JST | Actions → Issue 110 | `hq-instruct:` / `overlay-filled:` / `approved-yen:` |
| 人間が A8 を開いた日 | ナオミチ | 見えた確定だけ Issue 109 に `A8_YEN:` |
| Cursor が起こされた日 | Cursor | この表と Issue を照合し `output/sprint/TODAY.md` を1回更新して止まる |

プランニングの正は **今日のゲート**。dump が既に出している人間1手を Cursor が上書きしない。

## ゲート（JST）

円に直結する順。1日1ゲート。未達なら翌日も同じゲート。先の案件を足すな。

| 日付 | ゲート | 達成 | 未達の翌日 |
|---|---|---|---|
| **9/11** | neo 1語 | Issue 110 に `未提携` / `Threadsあり` / `YouTubeあり` / `項目なし` / `媒体なし` のどれか1つ | 同じ1語。FANZA・CW・XM に逃げるな |
| **9/12** | 1語の次 dump を1つ消化 | `未提携`→提携申請。`Threadsあり`→開設済み/未開設。`項目なし`/`媒体なし`/`YouTubeあり`→N高へ（neo は貼らない） | 同じ dump |
| **9/13** | 踏める面 | 転職プロフィールに neo を置いた、または neo を貼らず N高1語まで進んだ | プロフィールか N高。Secret より先 |
| **9/14** | overlay | `overlay-filled` ≥ 1（鍵名だけ。URL は Issue に出すな） | Secret の既存 JSON に鍵を足す。`links.json` は空のまま |
| **9/15** | 計測 | A8 画面を開いて `A8_YEN:`（見えた 0 は 0） | 開いていない日は行を足すな |
| **9/16** | N高 1語 | dump `G_hq_sns_nko.txt` の1語 | 教育アカに neo を置くな |
| **9/17–9/18** | アイズ → チケット | 各1語。置ける面だけ置く | 未開設アカに無理に置くな |
| **9/19** | 計測固定 | `G_hq_a8_csv.txt`。neo をチケットで上書きするな | 新しいプログラム ID を足すな |
| **9/20** | 中間 | overlay の鍵数と `approved-yen` を記録。入口がまだ 0 なら **入口不足** と書く（円を invent するな） | 入口が 0 なら 21 日以降も入口だけ。案件を増やさない |
| **9/21–9/27** | 既存面だけ | 既に置いたプロフィール／許可媒体。毎日 A8 を見た日だけ `A8_YEN:` | 自動投稿を戻すな。DMM アダルトを本線に足すな |
| **9/28–9/30** | 凍結計測 | 新規 dump 案件を足すな。見た円だけ | 期限を伸ばすな。目標を下げるな |

## 円の本線（これ以外はスケジュールに入れない）

1. 高単価を提携する
2. 人が踏める場所に広告リンクを置く（プロフィールが先。自動投稿は停止のまま）
3. 同じ URL を Secret に足す（Git に書くな）
4. A8 の確定を見たときだけ `A8_YEN:`

入れない: CW 応募数、カタログ 15000、XM ペーパー、H3、FANZA サイト構築、自動 DM、いいね自動化。

## Cursor が起こされた日の手順

1. Issue 110 の最新 `hq-instruct:` / `overlay-filled:` / `approved-yen:` を読む
2. Issue 109 の `A8_YEN:` を読む（無ければ円は 0 のまま）
3. 上の表で **今日のゲート** が達成かを書く
4. `output/sprint/TODAY.md` を日付ごと更新する
5. 仕組みの穴だけなら `参謀へ:` 1行。ログイン確認で起き続けるな
