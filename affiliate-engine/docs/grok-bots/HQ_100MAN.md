# HQ 100万 — 期限スコアボード

司令部（Grok Bot）の朝/夜レビュー用。工場の再構築ではない。数字は invent しない。ファイルに無いものは **ファイルに無い** と書く。

## 期限

| 項目 | 値 | 種別 |
|---|---|---|
| 期間 | 2026-08-26 〜 2026-09-30 | ナオミチ決定 |
| 目標 | ¥1,000,000 | ナオミチ決定。確定円。カタログ単価・再生は売上ではない |
| 残日数（2026-08-26 時点） | 36 | 今日〜期限当日を含む。`2026-09-30 − 今日 + 1` |
| 必要ペース | 目標÷残日数。実測ではない | 36日なら ¥1,000,000 ÷ 36。達成した円ではない |

期限後の残日数は 0。残日数を感覚で丸めるな。

## いまの実測円

**¥0。** `affiliate-engine/data/conversions.csv` に実測の成果行が無いあいだは 0 のまま。

| 見る列 | いま | ルール |
|---|---|---|
| `approved_yen` 合計 | 0 | コメント行（`date` が `#` で始まる行）は足さない |
| `clicks` 合計 | 実測行なし | **ファイルに無い**。0件と書いてあるわけではない |
| `cv` 合計 | 実測行なし | **ファイルに無い** |
| 再生・収益・CVR・EPC・フォロワー | — | **ファイルに無い**。他ドキュメントから転記するな |

不足円 = ¥1,000,000 − 実測円。いまは ¥1,000,000。カタログ報酬を足して埋めない。

## 20:00 JST HQ の読み順

1. **先に** `affiliate-engine/data/conversions.csv`
2. **そのあと** `affiliate-engine/data/video_cash_log.csv`（履歴行は残してある。円の合計には使うな）
3. このファイルに書いていない閲覧数・売上は **ファイルに無い**
4. 運用ナレッジ（目標は変えない）: `docs/grok-bots/HQ_GROK_OPS_KNOWLEDGE.md`

朝レビューも同じ。`video_cash_log.csv` のクリックを、この期限の実測円にしない。  
`HQ_GROK_OPS_KNOWLEDGE.md` は改善の切り方だけ。期限・¥1,000,000・実測円 0 は上の表が正。上書きするな。

## 決定済み（ピボット）

Furbo 購入10% / ペット癒しでは、この期限に届かない。根拠は現状 **11–50 クリック / 2ヶ月**（成果判定不能の母数）。量産や bot 分割では埋まらない。

寄せ先は **申込型ハイチケット**。数字はカタログであり、自前 CV ではない。実測円に足すな。

| 案件 | カタログ | 出典 |
|---|---|---|
| auひかり | 30,000 | `affi-shorts-agent/docs/OFFERS.md` |
| オリコ | 30,000 | 同上 |
| ガス屋 | 21–28k | 同上 |

このリポジトリに OFFERS.md の本文は置かない。カタログを「確定した」と書くな。

## 稼働面（今あるものだけ）

- YouTube `@pet_story_select`
- YouTube みくこんかつ
- Threads 5 accounts（`config/accounts.json` コメントの開設済み5。9キー全部が稼働面ではない）

閲覧数・登録者・売上は **ファイルに無い**。新しいチャンネルを開くな。YouTube 投稿を自動化するな。ジャンル剤のレシピ（`agents/` `ledger/`）をこのスコアボードのために編集するな。

## `video-cash-loop.md` との関係

実験ファイル `affiliate-engine/docs/video-cash-loop.md` は **消さない**。  
そこにある「週50クリックが3週続くまで月100万を語るな」は、ナオミチがこの期限のために上書きした。

**期限の売上トラッカーは本ファイル（HQ_100MAN）。** 14日クリック実験の手順は video-cash-loop に残す。

## 台帳の付け方

`conversions.csv` に1行足す。列: `date,source,program,clicks,cv,approved_yen,note`

- `source` は `A8` / `AdSense` / `other`
- ASP 管理画面で見た値だけ。空なら空のまま
- 例示の金額・仮の CV を書くな

## 運用ナレッジ（目標は変えない）

指令塔「月100万稼ぐまで帰れま10」が改善活動に使うファイル: [HQ_GROK_OPS_KNOWLEDGE.md](HQ_GROK_OPS_KNOWLEDGE.md)。

期限・目標円・実測円・ピボットは **本ファイルが正**。ナレッジ側で書き換えない。
