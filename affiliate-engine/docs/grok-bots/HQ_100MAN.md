# HQ — デフォルト clone 用

指令塔の clone は Grok Bot 1つ。日常のやり取りは Bot。Cursor は上流1体（台帳 `HQ_ROSTER.md` は Bot が開けるな）。数字は invent しない。CW 司令塔をこの clone に足すな。

今すぐ dump 1ファイルだけ開け:

`affiliate-engine/docs/grok-bots/dump/G_hq_boot.txt`

無ければ同じ本文: https://raw.githubusercontent.com/fireworker011/Research/claude/setup-colab-comfyui-Eb9Lh/affiliate-engine/docs/grok-bots/dump/G_hq_boot.txt

GitHub 指示役が Issue `Grok Bot — 指示` に `hq-instruct:` を書く（デフォルトへマージで初回、以後毎日）。既定は席1回 `G_hq_human_sitting.txt`。overlay が1以上なら計測。`AFFI: STOP` のときだけ XM。boot はそれを先に開け。毎日 dump を貼り直すな。Run workflow は不要。1語チェーンは使わない。

`G_hq_cw_remain.txt` と `G_hq_cw_n10.txt` は開けるな（駐車。引用1手は替えるな）。役割の正は `MACHINE.md`。

参謀の 9/30 ゲート表（Bot は開けるな）: `affiliate-engine/docs/grok-bots/HQ_CALENDAR.md`
台帳（Bot は開けるな）: `affiliate-engine/docs/grok-bots/HQ_ROSTER.md`

`HUMAN.md` は貼るな。他の `G_hq_*.txt` を更新日時で選ぶな。結合するな。cron は戻すな。Cursor は dump を貼らない。Cursor を常時稼働させるな。
