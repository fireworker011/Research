# HQ 100万 — 期限スコアボード

司令部と Cursor 24hスプリントの正本。数字は invent しない。ファイルに無いものは **ファイルに無い** と書く。

## 期限

| 項目 | 値 | 種別 |
|---|---|---|
| 期間 | 2026-08-26 〜 2026-09-30 | ナオミチ決定 |
| 目標 | ¥1,000,000 | ナオミチ決定。確定円。カタログ単価・再生は売上ではない |
| 残日数 | `2026-09-30 − 今日 + 1` | 今日〜期限当日を含む。感覚で丸めるな |
| 必要ペース | 不足円÷残日数 | 実測ではない。達成した円ではない |

計算は `node src/sprint-1m.js`。期限後の残日数は 0。

## いまの実測円

`affiliate-engine/data/conversions.csv` の `approved_yen` 合計。コメント行（`date` が `#` で始まる行）は足さない。

カタログ報酬を足して埋めない。クリックを円にしない。

## 指令の優先順位（2026-08-27〜）

1. **ナオミチ**（このスレッドに直接書いたこと）
2. このファイルの期限・目標・実測円
3. Grok Bot は数値の読み取りと「次の改善を1つ」まで。実投稿・スケジュール再開・案件変更を自分で実行しない

## 決定済み（ピボット）

Furbo 購入10% / ペット癒しでは、この期限に届かない。寄せ先は **申込型ハイチケット**。カタログは確定円ではない。実測円に足すな。

URL は Git に書かない。実リンクは GitHub Secret `AFFILIATE_LINKS_JSON` のみ。`config/links.json` の値は空文字のまま。

## 24時間スプリント（Cursor）

ナオミチ指示（2026-08-27）: この Cursor エージェントが 24 時間、毎時起きてスコアボードを更新し、次の1手だけ進める。

- Threads 自動投稿の cron は、リンク Secret が入るまで戻さない
- いいね / フォロー / DM / YouTube投稿の自動化はしない
- 体験談の捏造、#PR なしリンクはしない
