# 転職 Threads（チケット用・参謀・ハンドルをチャットに書くな）

指令塔がアイズを置けないと返したあと、または教育が `未開設` のあと。
neo の `Threadsあり` の直後には使うな（それは `EXIST.md` / `G_hq_threads_exist.txt`）。
neo を転職プロフィールに既に置いたなら使うな（同じ欄をチケットで上書きするな。次は `G_hq_banner_10.txt`）。
neo が `Threadsあり` かつ `未開設` なら聞かず（既知。次はバナー）。新媒体を今日開くな。cron は戻すな。

## 人間が返す1語

- `開設済み`（neo 未置き） → dump `G_hq_sns_ticket.txt`
- `未開設` → チケットは置くな。副サイト登録するな。Secret に入れるな。プロフィールに置くな。アカウントを新造するな。**止まれではない。** 次はバナー dump `G_hq_banner_10.txt`（出品するな）

## 見る場所

スマホの Threads で、表示名候補「けい｜しずかな転職準備」（`accounts.json` の key `tenshoku`）。
ハンドルはファイルに無い。チャットにハンドルも URL も書くな。

トークン未登録は未開設の証明ではない。画面でアカがあるかだけ。

## やらないこと

- 今日アカウントを新造する
- チケットを教育・ペット・副業に置く
- neo が入っているリンク欄をチケットで上書きする
- 転職 YouTube を始める
- ハンドル / プロフィール URL を Git / チャット / dump に書く
