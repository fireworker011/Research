# FINAL_CHECK — CW job 13405300 / 契約 63447842（ayk2125・旅行前日3選）

司令部の「明るめ Imagine 丸投げ文」と「組立方針」を、クライアント指示・IMAGE_PLAYBOOK・BOX_MEMORY と突き合わせた最終確認。
このファイルは判定だけ。投稿しない。納品しない。`agents/` 本文は直さない。PR95 の md 本文も直さない。

判定は `通す` / `落とし` / `INSUFFICIENT` だけ。きれいさで落とさない。

読んだもの（2026-09-06 10:27 JST）:

| 正本 | 場所 |
|---|---|
| 公開ジョブ | https://crowdworks.jp/public/jobs/13405300（再読。「募集は終了しています」。契約した人 4） |
| PR95 投げ文 | `affiliate-engine/docs/grok-bots/IMAGINE_THROW_CW_13405300.md`（ブランチ `cursor/cw-13405300-imagine-throw-cfd3` fe9b5cd） |
| IMAGE_PLAYBOOK / BOX_MEMORY | git `4dc3b3d`（ブランチ `cursor/video-channel-playbook-e013`）。本ブランチには無い |
| SpaceXAI 消費者向け利用規約 | https://x.ai/legal/terms-of-service（Effective: September 1, 2026） |
| 台本 | ayk2125 2026-09-02 11:48（PR95 §1.3 と同文。CW スレはログイン壁で再取得できていない） |
| 契約 63447842 の文面 | ログイン壁。測れていない。納期「本日から3日以内＝9/7目安」は司令部の申告どおりに扱う |

---

## 0. クライアント実測（公開本文。省略しない。報酬行は書かない）

- テーマ: 「旅行前日にやっておきたいこと3選」
- お願いしたい内容: 渡す台本をもとに、内容に合う素材を選び、縦型のリール動画に仕上げる
- 編集: 旅行イメージに合う素材選び ／ テロップの挿入 ／ 簡単な切り替えや動きの追加
- 素材: 使用する画像・動画素材は商用利用可能なもの
- 納品形式: 動画時間 10〜20秒程度 ／ サイズ 1080×1920 ／ 形式 MP4
- 納期: 契約後1〜3日程度（難しい場合は事前相談）
- 音声（ナレーション・BGM）の指定: 公開本文に **無い**
- 台本（ayk2125 2026-09-02。行を足さない。文言を直さない）:

```
■0〜2秒「旅行前日にやっておきたいこと3選」
■2〜5秒「① 天気予報をチェック」気温や天候に合わせて服装・持ち物を最終確認！
■5〜8秒「② 予約内容を再確認」ホテルや交通機関の時間・場所をチェック◎
■8〜11秒「③ スマホ・充電器を準備」スマホやモバイルバッテリーは前日に充電しておこう！
■11〜14秒「前日に確認して、旅行当日は安心して出発✈️」
```

---

## 1. 判定（項目ごと）

### 1.1 明るめ丸投げ文（行ごと）

| 行 | 判定 | 根拠 |
|---|---|---|
| `IMAGINE_THROW。投稿するな。CWに納品するな。文を足すな。` | 通す | PR95 冒頭・IMAGE_PLAYBOOK §6.7 と同じ |
| `CanvaとCWは閉じ、ブラウザ1つ、https://grok.com/imagine だけ。` | 通す | BOX_MEMORY §0・§3（画像生成 = grok.com/imagine だけ） |
| `Image。Quality (v2.0)。比が取れるなら 9:16。` | 通す | IMAGE_PLAYBOOK §6.3〜§6.5。取れないときの行は無いが、返却の W×H で司令部が比率違いを判定できる |
| `添付の起点画像5枚を1枚ずつ参照に使う（新しい顔を作るな。商標・ロゴ・読めるパッケージは出すな）` | INSUFFICIENT | 1枚ずつ = 公式の参照上限3枚の内側（IMAGE_PLAYBOOK §3）。ただし IMAGE_PLAYBOOK §6.6「依頼に既存画像が無いなら載せない」・PR95 §2.8「参照画像は載せない」と食い違う。クライアントは画像を渡していない。起点画像5枚を載せる判断は司令部が明記すること。加えて「画像に文字を出すな」が無い（§7 文字崩れ: 指定が無いのに文字があれば落とし。テロップは後載せ） |
| `各カットで明るめ・旅行テンション上がる雰囲気を保ったまま、縦9:16の静止画を1枚ずつ作れ。5枚混ぜるな。1枚終わったら次。` | 落とし | Imagine の入力欄に貼る文がこの丸投げ文の中に **無い**。1行目「文を足すな」と矛盾し、エージェントが自分で文を作る。IMAGE_PLAYBOOK §6.7「§8 のプロンプトを投げる。文を足すな」の形（貼る文は司令部が渡す）になっていない |
| `Error code 9 で落ちたら開き直すな。URLと1語。` | 通す | BOX_MEMORY §4 は「開き直しは1回。2回目も落ちたら INSUFFICIENT」。「開き直すな」はその内側で、より厳しい。Error 9 の公式意味は INSUFFICIENT のまま（意味を書いていないので可） |
| `返すのは画像ファイルと生成W×Hだけ。目鼻口が見えたら通すな。読めるブランドがあれば落とし。ログインが要るときは箱をナオミチに渡せ。` | 落とし | 落とし3項（IMAGE_PLAYBOOK §7）のうち **文字崩れ** のゲートが無い。顔・商標だけ |
| `CUT1 参照=cut1 荷物（0-2秒）` 〜 `CUT5 参照=cut5 出発（11-14秒）` | 通す | 台本5ブロックと秒が一致。CUT1（題）は台本に物の指定が無いので荷物は「旅行イメージに合う素材」の範囲。CUT2 天気予報・CUT3 予約・CUT4 充電・CUT5 出発✈️ は台本どおり |

### 1.2 組立方針

| 項目 | 判定 | 根拠 |
|---|---|---|
| 尺 14秒 | 通す | 公開本文 10〜20秒程度の内側。台本の最終ブロックが 11〜14秒 |
| テロップは台本どおり | 通す（方針）／ 実物は INSUFFICIENT | 方針は公開本文「テロップの挿入」と一致。組立済み MP4 はこの箱に無いので1字照合は未実測（§5 で測る） |
| 1080×1920 MP4 | 通す（方針）／ 実物は INSUFFICIENT | 公開本文と一致。実物は ffprobe で測る（§5） |
| ナレーション無し | 通す | 公開本文に音声の指定が無い（§4） |
| 簡単な切り替え | 通す（PR95 §4 に「フェードまたはカット」有り） | 司令部の組立方針の一文には無いが PR95 §4 が書いている |
| 動きの追加 | INSUFFICIENT（漏れ） | 公開本文「簡単な切り替えや動きの追加」。組立方針・PR95 §4 のどこにも「動き」の行が無い。静止画5枚を硬いカットで並べただけなら、この項は落とし |
| 商用利用可能な素材 | INSUFFICIENT | 公開本文の必須条件。Imagine 出力の商用可否の公式一文は IMAGE_PLAYBOOK に無い（PR95 §1.1 も同じ）。SpaceXAI 消費者規約（2026-09-01 施行）に公式にあるのは次だけ: Output の所有権はユーザーが保持（"You Own Your User Content"）／ Output を使うときは許可取得と Service への帰属表示が要る（"you are required to obtain our permission and attribute your generation of the Output to the Service, as detailed in our Brand Guidelines"）／ AI 生成の表示（"AI-Generated Disclosures"）／ 透かしは付く・外す設定は無い（FAQ・AUP。IMAGE_PLAYBOOK §3）。「商用可」の一文は **無い**。組立済みの静止画は GenerateImage 経由で、その出力の商用可否は本ファイルの正本に **無い** |
| 明るめの雰囲気（PR95 の夜寄りから変更） | 通す | 公開本文は「旅行イメージに合う素材」だけ。夜／明るめの指定は無い。どちらも違反ではない。ただし PR95 のフェンスは Night のまま。どちらを正とするか司令部が1つ決める |
| 顔なし・物と手 | 通す | 確定制約（新しい顔を作るな・他人の顔）。公開本文に人物の指定は無い |
| 納期 9/7 | 通す（期限内なら） | 公開本文「契約後1〜3日程度」。契約文面は未測。司令部申告の 9/7 を採る。今 2026-09-06 10:27 JST |

---

## 2. クライアント指示との漏れ一覧

| # | 漏れ | 公開本文の該当 | どこに無いか |
|---|---|---|---|
| 1 | **動きの追加** | 「簡単な切り替えや動きの追加」 | 組立方針の一文・PR95 §4 のいずれにも「動き」（ズーム／パン／テロップの出現）の行が無い |
| 2 | **商用利用可能な素材の根拠** | 「※使用する画像・動画素材は、商用利用可能なものをご使用ください。」 | 丸投げ文・組立方針のどこにも、生成元（Imagine / GenerateImage）の商用可否 URL が無い。公式に「商用可」の一文は見つかっていない（§1.2） |
| 3 | **画像に文字を出すな**（テロップと衝突） | 「テロップの挿入」（テロップは組立で後載せ） | 丸投げ文に無い。Imagine が画像に文字を焼くと、テロップと二重になり §7 文字崩れ |

漏れではないが司令部が1つ決めるもの: 参照画像5枚を載せる（IMAGE_PLAYBOOK §6.6 / PR95 §2.8 と食い違い）／ 夜寄り（PR95）と明るめのどちらを正にするか。

---

## 3. 明るめ丸投げ文 — 直す行（全文差し替え）

直した行は 4・5・7 行目と、末尾のフェンス1つだけ。他の行はそのまま。

直した理由（1行ずつ）:

- 4行目: 「画像に文字を出すな」を足した（§7 文字崩れ。テロップは後載せ）。
- 5行目: 「下のフェンス内だけを貼り」に変えた（貼る文が無いと「文を足すな」が成立しない）。
- 7行目: 「画像上に文字があれば落とし」を足した（落とし3項の欠け）。
- 末尾: 全カット共通の貼る文を1つだけ足した。被写体は参照画像が持つので、カットごとに文を分けない。夜の語は入れていない。

```
IMAGINE_THROW。投稿するな。CWに納品するな。文を足すな。
CanvaとCWは閉じ、ブラウザ1つ、https://grok.com/imagine だけ。
Image。Quality (v2.0)。比が取れるなら 9:16。
添付の起点画像5枚を1枚ずつ参照に使う（新しい顔を作るな。商標・ロゴ・読めるパッケージは出すな。画像に文字を出すな）。
各カットで参照1枚を付け、下のフェンス内だけを貼り、明るめ・旅行テンション上がる雰囲気を保ったまま、縦9:16の静止画を1枚ずつ作れ。5枚混ぜるな。1枚終わったら次。
Error code 9 で落ちたら開き直すな。URLと1語。
返すのは画像ファイルと生成W×Hだけ。目鼻口が見えたら通すな。読めるブランドがあれば落とし。画像上に文字があれば落とし。ログインが要るときは箱をナオミチに渡せ。
CUT1 参照=cut1 荷物（0-2秒）
CUT2 参照=cut2 天気予報（2-5秒）
CUT3 参照=cut3 予約確認（5-8秒）
CUT4 参照=cut4 充電（8-11秒）
CUT5 参照=cut5 出発（11-14秒）
```

貼る文（全カット共通。Imagine の入力欄にはこのフェンス内だけ）:

```
Still image. Vertical 9:16. Photoreal. Keep the attached reference's subject, framing, and bright daytime pre-trip mood. No head. No eyes, no nose, no mouth. No celebrity. No named character. No on-screen text. No captions. No letters in any language. No logos. No brand names. No trademarks. No product packaging with readable marks. No airline livery. No UI. No existing characters.
```

参照画像5枚を載せる判断は上の文に含めていない（§1.1 INSUFFICIENT）。載せるなら、載せる前に5枚とも 目鼻口 無し／読める文字 無し／読めるブランド 無し を司令部側で確認する（§5 #7）。

---

## 4. ナレーション要否（公開本文の根拠のみ）

**不要。**

- 公開本文の編集項目は「旅行イメージに合う素材選び」「テロップの挿入」「簡単な切り替えや動きの追加」の3つだけ。ナレーション・音声・読み上げ・BGM の語は **無い**。
- 納品形式は「動画時間」「サイズ」「形式（MP4）」の3つだけ。音声の条件は **無い**。
- 台本（2026-09-02）は5ブロックとも「」内のテロップ文と補足文で、読み上げ指示は無い。
- CW スレ（メッセージ）はログイン壁で再取得できていない。スレに音声の指示があるかは **INSUFFICIENT**。公開本文だけを根拠にすると不要。
- BGM も公開本文に無い。無音で納品するか BGM を敷くかは公開本文では決まらない。敷くなら §5 #6 のライセンス確認が要る。

---

## 5. 納品前チェックリスト（実測できる項目だけ）

すべて「測った値」を書く。測れない項目は INSUFFICIENT にして納品判断へ回す。

| # | 項目 | 測り方 | 通す条件 |
|---|---|---|---|
| 1 | 解像度 | `ffprobe -v error -select_streams v:0 -show_entries stream=width,height -of csv=p=0 <mp4>` | `1080,1920` |
| 2 | 尺 | `ffprobe -v error -show_entries format=duration -of csv=p=0 <mp4>` | 10.0 以上 20.0 以下（台本どおりなら 14.0 前後） |
| 3 | 形式 | `ffprobe -v error -show_entries format=format_name -of csv=p=0 <mp4>` に `mp4` が含まれる。拡張子 `.mp4`。最後まで再生できる | 3つとも満たす |
| 4 | テロップ 1字照合（8本） | 該当秒で一時停止して書き写し、次と1字も違わないこと。豆腐（□）が無い: `旅行前日にやっておきたいこと3選`(0〜2) ／ `① 天気予報をチェック` と `気温や天候に合わせて服装・持ち物を最終確認！`(2〜5) ／ `② 予約内容を再確認` と `ホテルや交通機関の時間・場所をチェック◎`(5〜8) ／ `③ スマホ・充電器を準備` と `スマホやモバイルバッテリーは前日に充電しておこう！`(8〜11) ／ `前日に確認して、旅行当日は安心して出発✈️`(11〜14) | 8本すべて一致。①②③ ！ ◎ ✈️ が表示されている |
| 5 | テロップ以外の文字 | 各カットの静止画に、テロップ以外の読める文字が無い | 無い |
| 6 | 音声 | `ffprobe -v error -select_streams a -show_entries stream=codec_name -of csv=p=0 <mp4>`。ナレーションは無し。音声トラックがあるなら中身（BGM）と、その素材の商用可 URL を1つ記録 | 無音、または BGM の商用可 URL が有る |
| 7 | 顔・商標（5カット） | 各カットで 目・鼻・口 が見えない／読めるブランド名・ロゴ・®・™ が無い | 5カットとも無い |
| 8 | 切り替え | 2・5・8・11 秒付近で画が替わる（カットまたはフェード）。黒フレームで埋めていない | 4箇所とも替わる。黒埋め無し |
| 9 | 動き | 各カットに ズーム／パン／テロップの出現 のいずれかが有るか。無いカットは記録 | 「動きの追加」に当たる動きが少なくとも1つある。無ければこの項は落とし（公開本文） |
| 10 | 素材の商用可 | 5枚の生成元（GenerateImage / Web Imagine）を記録し、その生成元の「商用利用可」を書いた公式 URL を1つ添える | URL が有る。無ければ INSUFFICIENT（公開本文の必須条件を未確認のまま納品する判断は司令部） |
| 11 | 透かし・AI生成表示 | 静止画または動画に透かし・ロゴ・生成表示が有るか記録。外さない・隠さない（AUP） | 記録した。外していない |
| 12 | 先頭・末尾 | 0秒と 14秒前後に黒だけのフレームが無い | 無い |
| 13 | Git | リポジトリに mp4 / png を置いていない（`git status` で未追跡のバイナリが無い） | 無い |
| 14 | 納期 | 納品時刻（JST）を記録 | 2026-09-07 JST 以内（契約後1〜3日。契約文面は未測） |
| 15 | 動作 | 投稿していない。CW への納品ボタンは司令部の判断が出るまで押していない | いいえ／いいえ |

---

## 6. 測れていないこと（INSUFFICIENT）

- 契約 63447842 の文面（ログイン壁）
- CW スレの音声・BGM に関する指示の有無（ログイン壁）
- 組立済み MP4 の実測値（この箱に無い。§5 で測る）
- Imagine / GenerateImage 出力の「商用利用可」を書いた公式一文
- 参照画像5枚を載せることの正本上の根拠（IMAGE_PLAYBOOK §6.6 / PR95 §2.8 は載せない）
- 「動き」がどの程度あれば「動きの追加」になるかの数値（公開本文に無い。有／無だけ記録）
- Error code 9 の公式意味（BOX_MEMORY のまま）
