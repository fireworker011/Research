# dump の仕分け（Bot はこの README を開けるな。hq-instruct の1ファイルだけ）

管理者は `G_hq_admin.txt` を一度開いて担当者へ実施。日常 clone の入口は `G_hq_boot.txt`。
Issue `Grok Bot — 指示` の `hq-instruct:` が指名した **1ファイル**だけ開け。管理者 dump は指名しない。
このディレクトリを更新日時で漁るな。結合するな。remain / n10 は開けるな。

## LIVE

| ファイル | いつ |
|---|---|
| `G_hq_admin.txt` | Grok 管理者（`b6bf30a6-a4f5-41ca-a2db-b417d936b9f4`）に一度。読んで担当者へ実施。指示役は指名しない |
| `G_hq_boot.txt` | 日常 clone（担当者）に一度。毎日貼り直すな |
| `G_hq_human_sitting.txt` | overlay が空。席1回。1語は返すな |
| `G_hq_a8_csv.txt` | overlay ≥ 1。見た円だけ |

XM は STOP のときだけ `xm-trade-engine/docs/grok-bots/G_xm_trade.txt`（このフォルダの外）。

## PARKED（開けるな。中身の1手は替えるな）

- `G_hq_cw_remain.txt`
- `G_hq_cw_n10.txt`

## ONE_WORD（アーカイブ。席に畳んだ。テストがパスを見るのでファイルは残す）

`G_hq_sns_next.txt` から始まる1語チェーン（partner / threads_exist / secret / profile / nko / eyes / ticket / yt_only ほか）。
指示役はこれを指名しない。自分で選んで開けるな。
