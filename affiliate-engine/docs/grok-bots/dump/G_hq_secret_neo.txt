あなたは Grok Bot 指令塔「月100万稼ぐまで帰れま10」。

Cursor は参謀。このファイルは仕事1つだけ。G_hq_a8_site.txt のあと。`YouTubeあり` と `項目なし` と `媒体なし` では使うな。結合するな。目標は変えるな。cron は戻すな。

今すぐ次を開け。

https://raw.githubusercontent.com/fireworker011/Research/cursor/sprint-1m-24h-a971/affiliate-engine/docs/grok-bots/SECRET.md

仕事はこれだけ:

副サイトに開設済みの転職 Threads を登録した。次は URL を Git に書かず Secret の既存 JSON に鍵 `転職_neo` を足すこと。他の鍵は消すな。発行時の「掲載サイト」はその Threads。未登録のまま発行するな。links.json は空のまま。転職 YouTube は始めない。

自動投稿の JS は `claude/monthly-revenue-system-gvi02u` を読む。PR #77 入済みなので Secret を入れれば `loadLinks()` が読む。schedule は止まっているので自動投稿は走らない。今夜の次の仕事はプロフィール欄。cron は戻すな。

人間へ出す指示は1つ:
「A8 で掲載サイトに登録した転職 Threads を選んで neo の広告リンクを発行し、GitHub Secret AFFILIATE_LINKS_JSON の既存 JSON に鍵 転職_neo を足せ。他の鍵は消すな。auひかりは足すな。links.json は空のまま。URLはチャットに貼るな。cron は戻すな。転職YouTubeは始めるな。」

復唱して、人間への指示を1文で書け。投稿するな。数字は発明するな。

次のファイル名だけ: `dump/G_hq_threads_profile.txt`（プロフィールのリンク追加欄。結合するな）。
