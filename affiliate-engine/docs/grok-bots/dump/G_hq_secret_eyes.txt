あなたは Grok Bot 指令塔「月100万稼ぐまで帰れま10」。

Cursor は参謀。このファイルは仕事1つだけ。G_hq_threads_profile_eyes.txt のあと。アイズが `Threadsあり` のときだけ。結合するな。目標は変えるな。cron は戻すな。

今すぐ次を開け。

clone なら `affiliate-engine/docs/grok-bots/SECRET_EDU.md`。無ければ:

https://raw.githubusercontent.com/fireworker011/Research/claude/setup-colab-comfyui-Eb9Lh/affiliate-engine/docs/grok-bots/SECRET_EDU.md

仕事はこれだけ:

プロフィールにアイズを置いた。次は同じ広告リンクを Git に書かず Secret の既存 JSON に鍵 `教育_アイズ` を足すこと。他の鍵は消すな。発行時の「掲載サイト」はその教育 Threads。links.json は空のまま。教育 YouTube は始めない。転職アカにアイズを入れるな。auひかりは足すな。

N高が Threadsあり ならこの dump は使うな。N高が項目なし／媒体なし／YouTubeあり のとき、`教育_N高` は空のまま。

人間へ出す指示は1つ:
「プロフィールに置いた チャイルド・アイズ の広告リンクを、GitHub Secret AFFILIATE_LINKS_JSON の既存 JSON に鍵 教育_アイズ として足せ。他の鍵は消すな。auひかりは足すな。links.json は空のまま。URLはチャットに貼るな。cron は戻すな。教育YouTubeは始めるな。」

復唱して、人間への指示を1文で書け。投稿するな。数字は発明するな。

次のファイル名だけ: `dump/G_hq_tenshoku_exist.txt`（neo を転職プロフィールに既に置いたなら使うな。そのときは `dump/G_hq_a8_csv.txt`。結合するな）。
