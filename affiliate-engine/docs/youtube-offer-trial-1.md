# 案件試験 1本 — ペット保険の資料請求

人間の決定: 2026-09-09 に **A**（安い／申込型を1本）。カメラ動画は増やさない。量産しない。媒体は足さない。

チャンネル: YouTube `@pet_story_select`
台本ID: `pet_offer_trial_1`
着地キー: `ペット_保険`（URLはGitに置かない。プロフィールだけ）
タイトル案: 病院代の前に見る3つ

## なぜこれか

- A8公開のペット欄（2026-08-10）は購入型が多い。Furboは購入10%、本体はセール時でも数千円〜通常数万円。
- 申込型でチャンネルの癒しShortsと衝突しにくいのは、ペット保険の資料請求（無料・特定社を推さない）。
- 火葬の電話問合せは成果地点は浅いが、癒しチャンネルの着地には使わない。
- フード初回購入はカメラより安いが購入型。今回は申込型だけを1本試す。

報酬額・EPC・確定率はA8管理画面を見るまで書かない。

## 人間が投稿前にやること（この順）

1. A8で「ペット保険」「資料請求」を検索する。成果地点が**資料請求**のプログラムを **1つだけ** 提携する。特定社を「一番おすすめ」と書いている案件は避ける（比較サイトの一括請求型が安全）。
2. チャンネル概要の外部リンクを、その資料請求URLに差し替える。**この試験中はカメラ用リンクと並べない**（クリックの着地が混ざる）。概要のPR表記はそのまま。
3. 下の台本で Shorts を **1本だけ** 撮る。映像の型（癒し）は変えてよいが、新しいジャンルにはしない。末尾で1回だけ「詳しくはプロフィールのリンク（PR）」。
4. 説明欄は下のブロックをそのまま。固定コメントにURLは置かない。既存32本は触らない。
5. `data/video_cash_log.csv` にその日の1行。note は `offer-trial-1 insurance`。数字が無い日は空欄のままにしない。
6. 同じ台本で2本目を出さない。判定はCSV。成果が出るまで案件を増やさない。

映像の型は変えない必要はない（これは癒し3本の再掲ではない）。**本数だけ1本。**

テロップ／読み上げ:
```
動物病院の会計、桁が違うことがある。

入る・入らないの前に、この3つだけ見る。
1. 補償割合。何割戻るか
2. 対象外。予防や既往は出ないことが多い
3. 待機期間と、入れる年齢

資料請求は無料。パンフレットを取り寄せてから決めてよい。

詳しくはプロフィールのリンク（PR）
```

YouTube説明文（URLなし）:
```
動物病院の会計、桁が違うことがある。

入る・入らないの前に、この3つだけ見る。
1. 補償割合。何割戻るか
2. 対象外。予防や既往は出ないことが多い
3. 待機期間と、入れる年齢

資料請求は無料。パンフレットを取り寄せてから決めてよい。

詳しくはプロフィールのリンク（PR）
#PR
```

生成: `npm run youtube:offer1`

