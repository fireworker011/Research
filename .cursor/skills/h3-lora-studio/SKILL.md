---
name: h3-lora-studio
description: MiniMax H3 LoRA studio for Colab. Daily エロ汎用 is mystic 0.5 plus penis 0.45 plus synth 0.4 plus turbo (Larry daily, LightX2V preview). Cinema off. Mystic alone paints balls. Adult T2V/I2V stacks Mystic XXX concept plus act + helper-or-cinema + optional thin turbo. Mystic does not replace act LoRAs. Futa blowjob may use two helpers plus thin Larry 6step. Anal sex uses anal-any-h3 as act when Drive has the weight, else penis + synth, no ThumbInButt (pose from prompt). Urine drink is penis + synth. Scat act stacks ThumbInButt so feces leaves the anus. ThumbInButt on anal fingering and scat. Use when the user names h3-lora-studio, Colab LoRA, Larry, LightX2V, cinematic DY, or select_loras.py.
---

# h3-lora-studio

質問しない。Grok は先に `h3-lora-studio/GROK_PROMPTS.md` と `h3-lora-studio/dump/G_h3_prompts.txt` を読む（このエロ動画の GitHub プロンプト全文）。長い会話の続きは `h3-lora-studio/HANDOVER.md`。Colab 実装なら `minimax_h3_lora_studio.ipynb`。起点の服抜き・セックスは別ノート `qwen_image_edit_nsfw.ipynb`（L4・`python colab/_write_qwen_edit_nb.py`。顔と画風の固定は必須。変えるのは服・姿勢・場所・行為。小便は亀頭先の黄色い水。脱糞は肛門から今出す固形。画風欄は変換しない。基本フタナリ。男禁止。H3 と同時に動かすな。ipynb は手で直すな）。設定だけなら `select_loras.py`。服ありAI起点のフタナリ全裸化は `h3-lora-studio/scripts/qwen_edit_nsfw.py`（Mk1227 `/infer`・576x1024・steps=4）。実写の他人は全裸化するな。JPGはGitに入れるな。ノートは `python colab/_write_lora_studio_nb.py` で再生成。`colab/h3_lora_studio.py` と `minimaxh3/h3_lora_studio.py` は同期する。ココナラ homage ノートの Turbo 既定は変えない。③の初期値は **物語＝登校／再生＝つなぐ**＋テキストから（②の今使うシーンは登校（専用）。訪問販売/検診/終点を初期値にしない）。`prepare_story_clip(..., force_t2v=True)` は Drive の試験 jpg を無視する。普通のつなぐ 20〜120秒は 1シーン＋長さで last-frame I2V（`rewrite_chain_opening_prompt` / `continue_chain_prompt`）。②の2回目は **設定だけ更新（既定オン）** で文章・JSONだけ取り直す。土台・LoRAの再取得と Drive の全部一覧は飛ばす。初めて／H3 Eros Max が無いときだけ全部入れる（公式 FL2VA だけでは飛ばさない）。説明書は GitHub の tar 一括。ローカルコピーは土台だけ（H3 Eros Max TURBO-hybrid beta5 int8・文字・VAE）。LoRA は Drive のまま。ref2va は②「参照土台も入れる」（既定オフ）または今使うシーンに「参照」があるときだけ。途中再開（`.part`）。公式 FL2VA は普通の I2V/T2V ノート用。Eros TURBO-hybrid では Larry / LightX2V FL2VA を積まない（焼き込み）。

専用ストーリー（`STORY_IDS`、帰宅〜縁側の11話）は ③ で5パターン（`resolve_story_play` / `apply_story_play`）:
- `〜（専用）` = カット。JSON のまま。`CHAIN=False`・`last_frame=None`。旧名「登校120秒（専用）」は `SITUATION_JA` のエイリアスで dedicated
- `〜（つなぐ）` = 文を直さずつなぐ。last-frame I2V。`rewrite_chain_opening_prompt` も最後の `rewrite_final_scene_i2v_prompt` もオフ。Picture 1 ロック（`continue_chain_prompt`）だけ必須
- `〜（つなぐ修）` = 文を直してつなぐ。last-frame I2V ＋ 1本目に `rewrite_chain_opening_prompt`。③「最終シーン合わせ」オンなら最後の本だけ `rewrite_final_scene_i2v_prompt`
- `〜（参照つなぐ）` = つなぐと同じだが **1本目は R2V**（Drive `input/cast/` の bust+full を identity 参照。I2V の最初のコマではない。FL2VA の竿は載せない。穴 `synth-pussy-h3` は載せる（竿役以外に竿が付くのを防ぐ）。口は blowjob＋穴＋Ref2VA turbo、歩行は cinema＋穴＋Ref2VA turbo、行為は AfterMidnight＋穴）。2本目以降は last-frame I2V（FL2VA の竿+穴スタック）
- `〜（参照つなぐ修）` = 参照つなぐ ＋ 1本目を長回しに直す。③オンなら最後の本だけ合わせる
- `短編集（参照）` = 10秒完結の超濃厚日常インモラル×複数。つなぎなし・連結なし。各本 **R2V**。メイン4人のうち竿役（レイ／マドカ）とハメ役（アヤ／サヤカ）の2人。画面は clip の `canvas`（フェラ9:16寄り、挿入16:9または立ち9:16）。文は `generate_immoral_shorts()`。部品は clip の situation の `stack_plan.r2v`
- `生成し直し` = 途中で止めた物語を、壊れた本から作り直す。③で作り直しの物語＋開始の本（1始まり）。開始より前は `output/stock/<id>/<stamp>/` にストックして連結。開始の本は前の最後のコマ（`h3_chain_{N-2}.png`）から I2V。専用で作っていても途中からならつなぐ（`apply_redo_play`）

名前付きパック（`CHAIN_PACK_IDS` = sales-visit-60s / checkup-100s / clinic-75s / last-stop-40s / last-train-120s / semen-bath-70s / meat-wall-85s / meat-wall-cesspit-70s ＋ 建前パック cafe-100s / train-sales-80s / red-light-50s / yoga-50s / back-wash-60s / karaoke-50s / laundromat-50s / lecture-desk-50s / camp-50s / fireworks-50s / cabin-40s ＋ **物語の追加** manhole-30s〜hachiko-30s）は専用ではない（`is_story` False、`is_chain_pack` True）が、③では**同じ5パターン**で再生する（`訪問販売（専用｜つなぐ｜つなぐ修｜参照つなぐ｜参照つなぐ修）` … `ハチコウ（…）`）。JSON は `kind:"chain"`・`seamless:true`・セリフは10秒（物語の追加も全部10秒）・無言の行為も10秒・LoRA は JSON の本ごと。画面サイズは JSON の `canvas`（`story_canvas_wh`）。clinic以外の全クリップ CAMERA は 16:9 PULLED BACK。膝上から両方の顔。顔が見えない寄り禁止。接合だけ寄り禁止（英語ロック: From the knees up: both faces readable. Not a face-hidden crop. Not a joining-only crop.）。終電は全部10秒。旧名「訪問販売60秒（つなぐ）」と裸の id は **つなぐ修**。画像サイズは JSON の `canvas` だけ（`story_canvas_wh`）。全シーンのふたなりは **玉なし＋マンコあり**（`lock_futa_anatomy`）。竿はボッキ時20cm・太い人間の綺麗な形で固定（`lock_futa_shaft`。ほぼ水平の軽い上反り、はっきりしたピンクの亀頭。馬・極細・フック・ねじれ・二股・こぶ・途中で変形しない）。訪問販売と定期検診は咥えやすい角度を重ねる（`lock_oral_easy_shaft`）。口にする台詞は話し言葉（漢字禁止。おチンチン・おミズ・ムク等の符丁はカタカナ）。シーンごとに WHO で誰が出て何をするかを書く。余り役は NOT IN FRAME。常時2人以上ではない。訪問販売は画面左手に販売員が閉扉の前に一人で立つ→チャイム→こんにちは→アヤ不出→あれ？トイレかな？おじゃましまーす！で右へ消える。10-20は玄関仰向けM字、アヤが自分の肛門アナニー。販売員あ、オナニーしてる！キモチよさそーう＋シコシコ。20-30は覆い被さりベロチュー→正常位膣挿入（肛門に入れるな）。30-50は正常位パコ。よだれ。んっ あっ はぁー。英語喘ぎ禁止。50-60は膣中出し。イキ顔。白は接合。60-70は膣から抜き。マンコから白。カエルポーズ。70-80はサービスでーす。黄色いおしっこを顔へ。白禁止。販売員は常時1人。帽子維持。正常位LoRA having missionary sex は c3-c5 に 0.4 まで。顔が消えたら外す。定期検診は画面左手に医師が家の玄関扉の前に一人で立つ→チャイム→右側の扉が開いてレイが右から入る。定期検診は入室後に短いフレンチキス＋ハグですぐ離れ、ヨロシクのあと恍惚の軽いキス、医師の後半（シツレイ／クチとムネ／カクニン／モンダイ）はエロ口語、ジュボは射精せず抜いて、無言で正常位（仰向け・膝を開く）アナル挿入オンカメラ→アナル中出し。最後は「ふふ、モンダイありすぎ。アナルにだされすぎ、、、」で口移しなし（本数は9のまま）。医院ケンシン（`clinic-75s`）は9本＝90秒。16:9 1024x576。アヤ受け＋医師竿。レイなし。騎乗なし。女医は仰向けにしない。0-10 こんにちは→pipe chair に SEATED しゃがみジュボ（ジュボ中も医師の顔がフレームに残る。PULLED BACK 16:9。lip-sync に `Full bodies from head to feet` を書くな）。10-20 10秒ずっとジュボ。竿は涎でテカる。口の端から涎。医師メチャクチャ気持ちよさそう。口を外すな。立つな。キスするな。無言。20-30 口を外す。医師笑顔ありがとう！ゴホウビです→先っぽから黄色い小便をアヤの顔へ。アヤは目を閉じ口を開け笑顔で顔と口で受ける。白禁止。精液禁止。カメラは医師の顔＋小便の線＋アヤの顔が16:9に入る。30-40 小便終わり。顔・髪・体が黄色い小便まみれ。おしっこ、あったかーい！ありがとー！→濃厚ベロチュー。40-50 向き転換→LOOKBACK→両手床→両膝→自分で尻を開く→医師が後ろに膝→先端が outer ring of the anus entrance に接触（未挿入）。50-60 同じ姿勢のままその輪から根元まで挿入。60-70 パコパコ。70-80 アナル中出し（白）→抜いて垂れ。80-90 ゲンキになりましたか／はい、スッキリしました。c5-c9は黄色い小便まみれを落とさない。洗うな。c5–c8: `30 degrees` + `SAME DIRECTION` + `NO KISS` + `PULLED BACK`。語 `rim` / `縁` はゼロ。口移しなし。気持ちよさそうなときは小さな声（`lock_pleasure_voice_and_wait`。言葉ではない）。**全物語共通**: 行為クリップ（ジュボ・口内・挿入・クンニ＝`ACT_SITUATIONS`）はセリフを足さない。無言のまま（`lock_act_silent`）。チンチンのジュボジュボ、チュー、唾液を舐める音、自然に漏れた喘ぎはちゃんと出す（`lock_act_sfx`。言葉ではない）。専用の会話本は短い一言＋待ちにせず、「」を長くして口を埋める（ユニーク上限2のまま）。行為以外の本は `lock_clip_timeline` で「」を1枠だけ。余り秒の無言Timelineは口／胸へのキス＋スキンシップ（同じセリフを二度三度言わせない）。既存の余りビート（跪きジュボ・口移し・SEPARATE）はそのまま。口が空いていて「この本はキスするな」と書いてある本は微笑と自己タッチまで。歩行だけ・入場（HIDDEN）・一人の暑さ（カフェあちぃー）・ハンドル／マイク／チョーク固定の手は触らない。飲尿シーンはジュボ（根元まで）に置き換えた（授業のトイレ放尿は見るだけ・残す）。家に医師が来る定期検診とは別。医院の医師だけ20cm。カフェ1本目は店員なし。店員は常時1人。二人目の店員・クローン・トレイ二つ禁止。飲尿は亀頭先の黄色い水。脱糞は人間の糞（ソーセージ状の固形。ゼリーやスライムではない）。ジュボ中は気持ちよさ、射精はイキ顔。建前パックは `spoken_max: 2`。物語の追加は 10秒×2＝20秒、台詞は1本目だけ、行為は無言、hmmotion は `futa_sex` だけ（アナル本には載せない）。ハチコウ（`hachiko-30s`）は夜の渋谷ハチコウ前。レイが画面左手で立ってフルボッキをシコシコ、アヤが右から現れてキス→口を開けて先端から手の幅→無言ジュボ口内（口が半分も保てず顔にすごい量。15秒禁止）。山小屋（`cabin-40s`）は糞まみれで扉の外のアヤ一人→便座でレイがシコシコ→ベロチュー→無言ジュボ→騎乗（マンコ）→後背→アナル挿入オンカメラ→アナル中出しはアナルから溢れる（`lock_anal_creampie`。マンコから出さない。アナル本に騎乗LoRAなし。顔入れ替え禁止）。セックス／口／クンニの直前の本は挿入寸前・受け入れる姿勢（先端から手の幅、未挿入／口を開けて先端から手の幅／膝を開いて舐め寸前）で終わる。3本目は足さない。行為→LoRA: ジュボ=oral（フェラ+竿+穴）、口内=oral_creampie（CUMOUF+竿+穴）、クンニ=cunnilingus_futa、アナル=futa_anal（anal-any-h3 があれば行為＋竿＋穴、無ければ竿＋穴だけ）、山小屋の騎乗（マンコ）=riding、山小屋の後背=doggy。`futa_sex` と AIO を使う本はもう残っていない（③のアナルに置き換えた）。射精は **ニクカベと同じ重油級の白いドロッドロ**（heavy-oil-thick opaque WHITE goo。色は白。黒・茶色のタールにしない。cum だけだと出ない。量は口が半分も保てないくらい溢れる。カーテン状に顎と胸へ。粘度は廃油／ヘドロ級の molasses-slow。肌と顔に残る、竿先から垂れてヌルヌル。`lock_semen_look`。風呂は同じ粘度の `BATH_LOOK`）。ジュボは竿舐め・先端咥えに逃げるので、咥え本は `lock_oral_in_mouth`（奥まで。20cmを根元まで咥える。唇は根元。喉。口内射精も根元のまま出す。竿舐め禁止。抜く本も frame 1 は根元、そこから一気に抜く＝`ORAL_PULL_OFF_LINE`）。セックス本は `lock_penis_inside`（勃起20cmがマンコまたはアナルの中。横・擦る・未挿入・軟は禁止。挿入を見せる本は ENTRY）。口内のあとは尺を増やさず、ジュボ側が相手と同じ目線に立ち上がって、白い粘つく液体を舌で絡める濃厚キス口移し（`lock_semen_share_kiss`。仰向けの相手は `on_back` で起き上がらせない。中出し・アナル・机の下・歩行は足さない）。**アナルの3パターン**: ③の選び画面に **①口内で終わる／②フェラのあとアナル／③会って即アナル** が並ぶ（汎用40秒。体位欄は②③だけ）。既存話はどれかが焼き込み。②③の締めは口移しなし（`SEMEN_SHARE_SKIP`）で「アナルあつい」系の話し言葉。休日と縁側だけ③のアナル中出しのあとハードカットで座り直し、①のジュボ→口内＋口移しを足して2回戦にする（アナル本では抜かない）。体位は場面で一番再現しやすいものを当てる（正常位・後背位・立ち後背位・騎乗位・後輩騎乗位・座位・後背位座位）。結合部が見える後背位／立ち後背位を多めにするが無理はしない。直前の本は必ず受け入れる姿勢＋穴から手の幅・未挿入で終わる。アナル本は Drive に anal-any-h3 があれば行為 0.55＋竿ヘルパー＋穴、無ければ Mystic 0.5 + 竿 0.45 + 穴 0.4 の3枚（cowgirl / riding-pose / doggy / AIO / Final Thrust / ThumbInButt / CUMOUF は積まない。Turbo なし・8step）。終点 10-20 は起こしのあと跪いてから咥える。終電（`last-train-120s`）は終点の延長（10本＝100秒・全部10秒。15秒禁止。車掌とレイの2人だけ。声かけは座っているレイに身体も顔も向ける。ジュボ20秒→口移しで起こす→ホームへ。濃厚キス→ピロートーク→立ちジュボで射精せず抜く→立ち後背アナル挿入→アナル中出し→抱擁（口移しなし）。車内の口内だけ①のまま口移しあり）。ザーメン風呂（`semen-bath-70s`）はおフロを白い粘液で満たす（口移しなし）。ニクカベ（`meat-wall-85s`）は巨大生物の体内の白いおフロ（コンクリートに肉を貼らない。歩行の床は弾力。足は沈まない。茶色い粘液は全身・顔・髪・チンチン・マンコ。白は廃油級のドロドロで色は白。レイは毎本フタナリ勃起20cm。竿は液から生えない。ジュボは浸かり深さ判定なし。0-10はうわぁ＋手繋ぎ＋軽いフレンチキス（咥え・ジュボではない）しながら歩く。ジュボは射精せず抜いて立ち後背アナル中出し（口移しなし）。家のザーメン風呂とは別）。ニクカベ肥溜め（`meat-wall-cesspit-70s`）は別バージョン（冒頭は全身白ザーメン→肥溜めに肩まで→頭から足先まで濃い茶色の糞まみれ。同じキス→ジュボ→射精せず抜いて四つん這いアナル中出し（口移しなし。糞まみれは全クリップ維持）。ジュボは深さ判定なし。素のニクカベの白いおフロとは別）。音声は `lock_spoken_japanese` が soundscape から lip-synced / No other speech / 台詞だけ を消し、効果音と「」だけにする。プロンプト本文は英語（`english_except_speech`）。「」の台詞だけ日本語。同じ「」は絵と音で各1つ。止めの日本語も ASCII の only-say-the-line も書かない（H3が音読した）。

**積まない（正しい未使用）:** `futa-h3-v51`（変身）、`anal-penetration-coachbate`（有料）、`photoreal-h3-still`（静止画）、`mystic-xxx-ref2va`（実験・音なし）、Ref2VA を FL2VA に、Larry+LightX2V、AIO+体位、③上級 extras。`riding-pose-i2v` は騎乗 I2V のみ（T2V は cowgirl）。`mystic-xxx-h3` は全 FL2VA T2V/I2V（日常エロ汎用含む）。単体は玉がつく。日常エロ汎用は歩行と同じく竿＋穴も積む。R2V・普通（エロなし）・電話ノートには載せない。行為 LoRA の代わりにはならない。ふたなりシーンは必ず穴ヘルパー `synth-pussy-h3` をセット（竿だけだとハメ役にも竿が付く）。歩行 `futa_visible` と日常エロ汎用の T2V/I2V はシネマを外して穴を載せる（cinema XOR helper）。

```bash
python h3-lora-studio/scripts/select_loras.py --list
python h3-lora-studio/scripts/select_loras.py --situation sfw_daily --mode t2v --prompt '（シーン）'
python colab/_write_lora_studio_nb.py
```

Fal に LoRA は差せない。成人 21+。

アナルセックス／飲尿は ThumbInButt も AIO も積まない（構図が死ぬ）。脱糞は ThumbInButt を積む（積まないとマンコから出る）。③のアナルは Drive に `anal-any-h3.safetensors` があれば行為 0.55＋竿ヘルパー＋穴、無ければ竿＋穴＋文章。指入れも ThumbInButt。既存話の飲尿はジュボのまま。医院・終電の `No feces.` は外さない。学習キットは `h3-lora-studio/train/`。準備と作業は `h3-lora-studio/train/CHECKLIST.md`。撮影リストは `h3-lora-studio/train/grids/`。スマホだけ（PC・ffmpeg なし）は `h3-lora-studio/train/PHONE.md`：③の体位欄＋`input/phone` の jpg、学習は体位フォルダ→④→fal。設定の版 `h3-20260914-anal-18`。root の `minimax_h3_lora_studio.ipynb` は Colab バッジ先。PLACEHOLDER にするな。Driveコピーや xxx ノートの②は GitHub を取り直さない。`cursor/h3-anal-stories-f112` の Open in Colab から開き直す。③は **シーン／物語／再生／ThumbInButt** の4欄（再生既定はつなぐ。TIB 既定オフ。物語が勝つ。行為・三択は物語を「（シーンのまま）」）。文章欄の下に **フリー文の部品**（物語JSONでは無視）。③の解説表（MD0/MD3）は `SITUATION_HELP` と同じ締めに揃える（口内終わりのまま残すな）。脱糞のデフォルト以外は③「脱糞（どの構図）」＋体位＋文章欄の場所。物語ドロップダウンは選ばない。山小屋／肥溜めは塗れ。医院・終電の `No feces.` は外さない。Mystic XXX v4 は全 FL2VA T2V/I2V の解剖スロット 0.5（日常エロ汎用含む）。単体は玉がつく。日常エロ汎用は竿 0.45 + 穴 0.4 も積む。行為 LoRA の代わりにはならない。普通（エロなし）と R2V と電話ノートは mystic なし。Ref2VA 版は実験・音なしなので載せない。

エロなし: Turbo1 + 画質1。専用「普通」と R2V は mystic なし。日常／最速／音残しはエロ汎用（nsfw）。Mystic 0.5 + 竿 0.45 + 穴 0.4 + Turbo。シネマなし。玉なし＋マンコあり。Larry と LightX2V は同時に積まない。FL2VA と Ref2VA を混ぜない。Photoreal still は動画本体に載せない。DY と ASTROCINEMA は同時に積まない。

エロ: 行為1 + 解剖0〜1 + ヘルパー0〜2 + Turbo0〜1。Eros 成人 T2V/I2V は mystic-xxx-h3 0.5。空欄は全裸のごく普通の若い成人女性（21+）。女かふたなりのみ。男厳禁。描写は③の文章欄。ふたなりフェラは汎用（空欄は第三者2ショットで根元。場所・座りは文章。体位欄は無視。POVにしない）。ヘルパー2（竿＋穴）+ Larry 0.5 / 6step。セックス（女体）/ アナル / 騎乗 / 後背位はヘルパー2で Turbo オフ。アナルセックス（女体）は解剖 0.5。Drive に anal-any-h3 があれば行為 0.55＋竿ヘルパー 0.45＋穴 0.4。無ければ竿 0.45＋穴 0.4。Turbo オフ・8step。ThumbInButt は積まない（四つん這い固定）。アナル挿入（画質）は同じ積みで res_multistep simple 8step。挿入本は結合部寄り（0-1.5秒キス／台詞、残りは挿入。つなぐ I2V は前カメラを捨てる）。構図は文章欄。手は腰。飲尿は竿＋穴。脱糞は ThumbInButt + 竿 + 穴（積まないとマンコから出る）。指入れも ThumbInButt（四つん這い・穴が膣より上）。空欄文に the man / his は書かない。体位 LoRA は AIO の代わり（同時に積まない）。騎乗 I2V は riding-pose-i2v 0.45 + 竿 + 穴 / 8step。T2V 騎乗は cowgirl 0.55 + 竿 + 穴 / 8step。後背位は doggy 0.55 + 竿 + 穴 / 8step。正常位POVは POV 0.55 + 竿 + 穴 + Larry 0.5 / 8step（横はセックス（女体））。後射精は HMCumshot 0.55 + 竿 + 穴 + Larry 0.5 / 8step（外出し。中出し・顔射とは別）。顔射は cmst + 竿 + 穴 + Larry 0.5 / 8step（後射精・絶頂・口内とは別。I2V本線）。中出しは Final Thrust 0.55 + 竿 0.45 + 穴 0.4 / Turbo オフ・8step（膣の中。学習文の male character は書かない。I2V本線）。口内射精は CUMOUF 0.4 + 竿 0.45 + 穴 0.4 + Larry 0.5 / 8step（口の中。顔射ではない。I2Vは口が付いた途中の写真）。指入れは膣。アナル指入れは ThumbInButt + 穴 + Larry 0.5 / 8step（I2V本線。指入れ・CoachBate・AIO・竿とは積まない）。指入れとオナニーは別シーン（同時に積まない）。足コキは Type D + 竿 + 穴 + Larry。絶頂は Remoteorgasm（射精ではない）。汎用エロ（女体）は解剖 0.5 + AIO 0.55 + 竿 0.45 + 穴 0.4 / 8step（Turbo オフ）。変身 LoRA は足さない。riding-pose-i2v は I2V 騎乗だけ（T2V に載せない）。秒数はワンショット 4〜15。物語チェーンの各本は10秒。ワンショット生成だけ最大15秒。チェーンを15秒にするな。つなぐ 20〜120秒は同じカットを最後のコマで I2V（③の「つなぐ 20秒」〜「つなぐ 120秒」。1本目は長回しにリライト。秒数欄は無視）。120秒は 10×12。任意の 16〜120 は「つなぐ（秒数欄・16〜120）」。2〜12本目は③のつなぎ欄（空なら前の続き）。帰宅・洗い物・登校・授業・屋上・おかえり・風呂・食卓・布団・休日・縁側の（専用）はカット編集（長さの作り方・つなぎ欄・秒数は無視）。（つなぐ）/（つなぐ修）は同じ JSON を last-frame I2V でつなぐ（長さの作り方・つなぎ欄・秒数は無視）。全話10秒・1本1場所1動作。セリフは口元が見える本だけ（リップシンク）。行為はLoRAのカメラ（口元／舌／接合点／膝元）。歩く本に行為部品を載せない。体位とカメラが合わない行為は捩じ込まない（仰向けの口にアナルやセックスを足さない）。登校・おかえり・風呂・食卓・布団・休日・縁側は10秒×12本・16:9。授業と屋上は10秒×10本・16:9。挿入 LoRA と SFW の速い＋綺麗は併用しない。訓練で体位を足さない。 専用の文は③で `compact_story_prompt()` を通す：画面に居ない人の全身描写を消して `Not in this clip: X.` にする、`Do not copy the previous clip`・`Clip N of M`・CAST LOCK の他話タイトルを消す、H3 正規順（subject_definitions → environment → integrated_multimodal_description → overall_soundscape → non_diegetic_music）に並べ直す。歩行（futa_visible）は解剖 0.5 + 竿 0.45 + 穴 0.4 + Larry 0.6 / 8step。`「」` のある口パク本は Larry を外して竿+穴・res_multistep 12step（シネマ＋口パクで顎が溶ける。`drop_speech_face_killers`）。音声は `lock_spoken_japanese` が soundscape から lip-synced / No other speech / 台詞だけ を消し、効果音と「」だけにする。同じ「」は絵と音で各1つ。止めの日本語も ASCII の only-say-the-line も書かない（H3が音読した）。③の再生グラフも同じ。フェラ（女体）は解剖 0.5 + Blowjob 0.6 + 竿 0.45 + 穴 0.4 + Larry 0.5 / 8step。ふたなりフェラは同じヘルパー2 + Larry 6step。口内は CUMOUF + 竿 + 穴 + Larry 8step。②の全部入れはディスク保存。再生グラフはその本の LoRA だけ。`/free` は OOM と FL2VA↔Ref2VA の土台切替だけ。LoRA の付け替えでは土台を下ろさない。トリガーは単語一致（`DY` が `body` に埋もれない）。`negative` は文書用（BasicGuider、CFG なし。除外は正の文に書く）。hmmotion は AIO セックス本だけ・先頭（validate_story_follow が検査）。重みと pip/torch キャッシュは Drive。生成はローカル SSD に載せてから（土台だけ `stage_models_to_local` + `warmup_h3_engine`。LoRA は Drive。ref2va は②の参照土台オンまたは参照シーンだけ。`.part` 再開）。Drive FUSE を mmap しない。
