name: サクラ起動キー銀行を Issue に掲示

# 本線は Grok Bot A が毎朝 raw URL で keys/<date>.md を読む。cron は要らない。
# ここは銀行を更新した時に Issue「サクラ起動キー」へ案内を書くだけ。
on:
  workflow_dispatch:
  push:
    paths:
      - 'sakura-ig/keys/CURRENT.json'
      - '.github/workflows/sakura_ig_handoff.yml'

concurrency:
  group: sakura-ig-handoff
  cancel-in-progress: false

jobs:
  handoff:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - name: 銀行を検品
        run: node sakura-ig/src/validate-keys.js

      - name: Issue に掲示
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          node sakura-ig/src/handoff.js
          test -f sakura-ig/output/handoff-latest.md
