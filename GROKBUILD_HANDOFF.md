# Affiliate Engine — grokbuild 引継ぎドキュメント

作成: Claude（保守セッション） → 引継ぎ先: grokbuild
最終更新: 2026-08-15

このファイルは `CLAUDE.md` の内容を土台に、**AI（モデル）が変わっても同じ運用品質を保つため**、
これまでの保守で得た暗黙知・失敗事例・判断基準までを含めて明文化したものです。
`CLAUDE.md`（不変条件の要約）と `affiliate-engine/README.md`（仕様書）はこのファイルと役割が重複しますが、
本ファイルは「なぜそうなっているか」「過去に何が壊れたか」に重点を置いています。3つは矛盾しないよう保守してください。

---

## 0. このシステムが何か（1分で）

Threads（Meta の SNS）9アカウントで、アフィリエイト投稿を**完全無人で**
投稿→計測→改善のループを回すシステム。GitHub Actions のみで完結し、ローカル実行・常駐プロセス不要。
人間の仕事は1日10分（Issueに届く返信下書きをコピペ送信するだけ）に設計されている。

目標（2026-08-04設定）: 8/31 初報酬 → 9/30 月次¥30,000 → 10/31 月次¥100,000。
未達時の撤退・転換ラインも README に明記済み（数字で判断する設計）。

**読む順番**: このファイル → `affiliate-engine/README.md`（仕様・セットアップ・ロードマップ）→
`CLAUDE.md`（不変条件チェックリスト）→ 必要に応じて `affiliate-engine/docs/*.md`（アカウント戦略・設営手順）。

---

## 1. 絶対に壊してはいけないもの（不変条件）

`CLAUDE.md` に列挙済みだが、**理由と実際に起きた事故**をセットで再掲する。理由を知らないと
「リファクタで良かれと思って」壊す事故が起きるため。

### 1-1. ブランチ運用とデプロイの仕組み
- 開発ブランチ: `claude/monthly-revenue-system-gvi02u`。デフォルトブランチ: `claude/setup-colab-comfyui-Eb9Lh`。
- **このリポジトリに「デプロイ」という概念はない**。全ワークフローは実行時に作業ブランチを checkout するため、
  作業ブランチに push した瞬間が本番反映。PRのマージ待ちではない。
- 例外1つだけ: `.github/workflows/*.yml` の **cron/トリガー定義**（`on:` セクション）は GitHub の仕様上、
  デフォルトブランチ上のYAMLでしか評価されない。ここだけはデフォルトブランチへのマージが必要。
- デフォルトブランチにマージした後、作業ブランチはデフォルトブランチから
  `git fetch origin <default> && git checkout -B <作業ブランチ> origin/<default> && git push --force-with-lease`
  で必ず再スタートすること（そうしないと次回以降の差分マージで混乱する）。

### 1-2. スケジュールCSVの日付決定論 — 実際に事故った例
- `strategy-engine.js` のテンプレ選択ロジックは「日付から計算で一意に決まる」設計（乱数・巡回カーソルなし）。
- **2026-07-07〜09に実際に発生した事故**: 生成のたびにリセットされる巡回カーソル（前回どこまで使ったかを
  ファイルに保存する方式）を導入したところ、スケジュールが**毎日再生成される**性質と噛み合わず、
  カーソルが常に同じ位置から始まり「毎日同じ投稿」が繰り返される事故になった。
- 教訓: このシステムの「状態」はほぼ全て `output/` 配下の JSON/CSV に永続化されるが、
  **生成ロジック自体は状態を持たせてはいけない**（= 冪等・純粋関数であるべき）。
  「今日は何番目の投稿か」は必ず日付から計算する（`util.js` の `todayJST` 等を使う）。
- 検証方法: `node src/strategy-engine.js --from-file data/seed_templates.json` を2回実行して
  出力CSVのmd5が一致することを確認する（`CLAUDE.md` にも記載済みの検証コマンド）。

### 1-3. 重複投稿ガード
- `threads-poster.js` は投稿前に `output/state/posted.json` の `recent`（直近7日分の内容ハッシュ）と照合し、
  一致したら投稿しない。このガードを外す/緩める変更（例: ハッシュの正規化を弱める、期間を短縮する）は
  同一文面の複数アカウント同時投稿という**スパム判定要因**に直結するため厳禁。

### 1-4. コンプライアンス検品の順序
- `compliance.js` の `validateTemplate()`（構造チェック）→ `checkContent()`（法令/規約チェック、#PR自動付与・NG表現ブロック）
  の**順序と両方必須**は変更しない。
- 理由: テンプレ生成はClaude（LLM）が行うため、構造が壊れたJSON片やNGワードが**モデルの気分次第で**混入しうる。
  検品をコードで強制することで「モデルの賢さに品質を依存させない」設計にしている。grokbuildが生成を担当する
  場合も同じ前提が成り立つ（LLM出力は信用せず必ずコードで検品する）。
- 法的根拠: ステマ規制（景品表示法、2023年10月施行）でリンク付き投稿への広告表記（#PR）は法的義務。
  外すとアカウント運用者本人が措置命令の対象になり得る。薬機法（「絶対痩せる」等の断定表現）も同様にブロック対象。

### 1-5. コスト構造で守っていること
- Claude APIは `insight.js`（日次リサーチ）と `engage.js`（返信下書き生成）のみが使用。
  `threads-poster.js`（投稿）と `report.js`（KPI集計）はAPI不使用（$0）。この分離は意図的なコスト設計。
- `config/budget.json` だけでAPIコストを制御する設計（スマホからでも編集可能にするため、
  コード変更なしで運用者が数値を触れる場所を1ファイルに集約している）。新しいAPI呼び出しを追加する場合は
  必ずこのファイル経由でパラメータ化すること。

### 1-6. やらないと決めていること（倫理・凍結リスク）— なぜ検討して却下したか
これは「思いつかなかった」のではなく「元構想にあったが意図的に削った」機能群。再提案する前に理由を読むこと。

| やらないこと | 元構想にはあった | 却下理由 |
|---|---|---|
| いいね/フォローの自動化 | あり | 公式APIが存在しない。非公式ツール（画面操作等）はMetaが最も積極的に検知する行為。5アカウント全凍結＝事業終了リスクに見合わない |
| 人間を装う自動返信・DM | あり（Sakura AI構想） | 欺瞞行為でMeta規約違反。発覚時は全アカウント凍結 |
| #PRなしのリンク投稿 | 元コードには広告表記なし | 景品表示法違反（法的リスク） |
| 体験談の捏造 | Claudeに「バズアカウント調査」させる構想があった | API経由のLLMはSNSを閲覧できないため、実在しないアカウント・数値を捏造する。捏造データでの戦略判断は実測より悪い（誤った自信を生む） |

grokbuildが「効率化のためにいいね自動化を追加しては」等を提案する場合は、上記の理由を踏まえて
**ユーザーに凍結リスクを明示した上で判断を仰ぐこと**。黙って実装しない。

---

## 2. アーキテクチャ（データフロー）

```
strategy-engine.js（週1回・手動）
  Claude でジャンル別テンプレ生成。output/top_posts.json（実測の勝ちパターン）を
  few-shot として注入 = 週次で型が進化する
  → output/threads_posting_schedule.csv（60日分、日付決定論）
        ↓
threads-poster.js（GitHub Actions cron・毎時23分）
  期日到来分のみ投稿して終了（ステートレス）。投稿前に compliance.js を通す。
  重複ガード（posted.json）。投稿後に amplify.js を実行
        ↓
amplify.js（threads-poster.js から呼ばれる）
  直近72時間の自アカ投稿を監視、500ビュー超（AMPLIFY_MIN_VIEWS）の投稿のリプ欄に
  自動でリンク付きリプライ（1アカ2回/日まで）。Claude API不使用
        ↓
report.js（GitHub Actions 毎日14時JST）
  Threads Insights + ASP成果CSV(data/conversions.csv) → 日次KPIレポート
  月末着地予測・目標との差・ジャンル別成績を算出
  → output/top_posts.json（次回のテンプレ生成に自動フィードバック = ループが閉じる）
```

```
insight.js（GitHub Actions 毎日06時JST、1日2ティック+冪等ガード）
  実測分析 × Web検索市場リサーチ（budget.jsonで日替わりNジャンルをローテーション）
  → 改善提案（テンプレ草案）
        ↓
apply-proposals.js（--auto で無人運転）
  草案を統合、実測ワースト3本を引退、ジャンルあたり36本上限でテンプレを更新
  → strategy-engine.js を再実行してスケジュール再生成
        ↓
engage.js（同ワークフロー内）
  返信・コメント下書きを生成 → GitHub Issue #13 にコメントとして投稿
  （人間は1日10分でコピペ送信するだけ）
```

補助スクリプト（任意・手動実行）:
- `funnel-calc.js`: 目標金額 → 必要ビュー数の逆算（意思決定の基準値を出すツール）
- `video-semi-auto.js`: 反応の良い投稿をShorts/Reels動画化（要ffmpeg + Noto CJKフォント）。
  自動投稿はしない（生成→人間が確認→公式ツールで予約投稿、という半自動設計）
- `refresh-tokens.js`: 長期トークンの週次更新（`refresh_threads_token.yml` から呼ばれる）

---

## 3. ファイル別の役割と触るときの注意

`affiliate-engine/src/` 配下（全て Node.js 20+ 標準機能のみ、依存パッケージなし・`npm install` 不要）:

| ファイル | 役割 | 触るときの注意 |
|---|---|---|
| `strategy-engine.js` | テンプレ・スケジュール生成 | 日付決定論を壊さない（§1-2参照）。`--from-file` でAPIキーなしでも動く経路を維持する |
| `threads-poster.js` | 自動投稿（ステートレス） | Threads APIは①コンテナ作成→②公開の2ステップが正規フロー。トークンはクエリパラメータで送信する仕様（コード内コメント参照）。`CATCHUP_HOURS`・`DAILY_CAP` は環境変数で調整可 |
| `compliance.js` | #PR付与・NG表現ブロック | `validateTemplate()`→`checkContent()`の順序と両方必須（§1-4） |
| `amplify.js` | バズ投稿への自動リンクリプライ | しきい値 `AMPLIFY_MIN_VIEWS`（既定500）。1アカ2回/日・二重リプなし・リンク未設定ジャンル除外・#PR必須、を崩さない |
| `report.js` | KPI収集・日次レポート・top_posts抽出 | ここが壊れると strategy-engine.js への学習フィードバックが止まる（サイレント劣化に注意） |
| `insight.js` | 実測×Web検索リサーチ→改善提案 | `budget.json` 経由でコスト制御。冪等ガード（1日2ティック）の仕組みを維持する |
| `apply-proposals.js` | 草案の統合・引退・スケジュール再生成 | `--prune 3 --cap 36` の引退ロジックはREADMEの数値と一致させる |
| `engage.js` | 返信下書き生成 | 送信は絶対にしない（人間が送る設計、§1-6） |
| `funnel-calc.js` | 目標からの逆算 | 意思決定の基準値。ロジック変更時はREADMEのロードマップ数値と整合させる |
| `video-semi-auto.js` | 動画化（半自動） | 自動投稿機能を追加しない（半自動が意図的設計） |
| `claude-client.js` | Claude APIラッパー（リトライ・JSON抽出） | LLM出力は信用しない前提のパーサ。JSON抽出の失敗時フォールバックを壊さない |
| `refresh-tokens.js` | トークン週次更新 | `GH_SECRETS_PAT` 未設定時はIssue通知にフォールバックする設計を維持（投稿を止めない） |
| `util.js` | CSV/JSON/日付ユーティリティ | `todayJST` 等の日付計算はJSTで統一（サーバーがUTCでも結果が変わらないように） |

---

## 4. よくある保守タスク（Runbook）

- **投稿されない**:
  1. GitHub Actions cronは1〜4時間遅延が常態（毎時起動で吸収済みの設計。数時間は待つ）
  2. Actions実行ログで `skipped_*` の理由を見る（`duplicate`/`no_link`/`awareness` は正常動作、異常ではない）
  3. トークン失効ならIssueが自動で立つ
- **コスト調整**: `config/budget.json` の数値を変えるだけ（翌朝反映、コード変更不要）
- **アカウント追加**: `accounts.json` に追記 + `links.json` にキー追加 + seedテンプレ追加（`validateTemplate()` を通すこと）
  + 4つのワークフローにSecrets env追加 + `engage.js` の PERSONAS/SEARCH_KEYWORDS + `insight.js` の link_key リスト
  （**1箇所でも漏らすとそのアカウントだけ動かない**。チェックリスト化推奨）
- **8/5リンク解禁**（フェーズ切替）: `links.json` にURLを入れておけば自動で収益投稿に切り替わる。追加作業なし
- **変更の検証手順（最低限、CLAUDE.mdと同一）**:
  ```bash
  cd affiliate-engine
  node --check src/<変更ファイル>.js
  node src/strategy-engine.js --from-file data/seed_templates.json   # 破棄警告が出ないこと
  node src/threads-poster.js --dry-run                               # 実投稿なしで内容確認
  # スケジュールの決定論性: 2回生成してmd5が一致すること
  ```
- **コミット手順**: `git pull --rebase` してから push（自動投稿の状態コミット `chore: 投稿状態を更新` 等と
  頻繁に競合するため。このリポジトリは自動コミットが1日に何度も走る）

---

## 5. 秘密情報の扱い

- トークン等は**GitHub Secretsのみ**。ファイル・コミット・ログに絶対に書かない。
- `refresh_threads_token.yml`（毎週月曜09:07 JST）が5アカウント分の長期トークンを自動更新。
  `GH_SECRETS_PAT`（Secrets: Read and write 権限のFine-grained PAT）があればSecrets自動書き戻し。
  未設定時は実行ログのSummaryにトークンを出力しIssueを自動作成 → 手動で貼り直す運用にフォールバックする
  （このフォールバックがあるため、PAT期限切れでも投稿自体は止まらない）。

---

## 6. コンプライアンス・法規制（アカウント生存戦略として理解する）

BAN・行政指導は収益ゼロに直結するため、機能ではなく「事業継続の前提」として扱うこと。

- ステマ規制（景品表示法、2023年10月〜）: リンク付き投稿への `#PR` 表記は法的義務
- 薬機法/誇大表現: 「絶対痩せる」「必ず稼げる」等の断定表現はNG
- Metaポリシー: アダルト・出会い系訴求は最速でBANされる。API投稿は公式Threads APIのみ使用
  （1アカウント250投稿/日の公式上限に対し既定10/日と大幅マージンを取る設計）
- 各ASPのオファーごとに「SNS投稿可否」規約が異なるため、新規提携時は必ず確認する（システムでは検知不可能な領域）

---

## 7. 用語・略語ミニ辞書（非日本語話者向け補助）

- ASP = アフィリエイトサービスプロバイダ（成果報酬の仲介事業者）
- CVR = コンバージョン率、CTR = クリック率
- JST = Japan Standard Time（このシステムの全時刻基準）
- 認知フェーズ / 収益フェーズ = `accounts.json` の `awareness_until` 前後のフェーズ名。
  認知フェーズはリンク付き投稿を自動除外、収益フェーズはlinks.json設定次第で自動混在開始
- ランプアップ = 新規アカウントの投稿数を1週目1本→2週目2本→3週目3本と段階的に増やす仕組み
  （急激な投稿増はスパム判定・リーチ抑制の要因になるため）

---

## 8. grokbuildへの申し送り事項

- 本システムは「LLMの出力を信用せず、必ずコードで検品・決定論化する」設計思想で一貫している
  （§1-2, §1-4, §1-6参照）。grokbuildが新機能を追加する際も、LLM生成物は必ず非LLMの検証層を通すこと。
- 意思決定の基準は常に**実測データ**（`output/top_posts.json`, `metrics_history`, `data/conversions.csv`）であり、
  推測・LLMの自己申告データを戦略判断に使わない（過去に構想としてあったが実測不可能なため却下した経緯あり、§1-6）。
- ロードマップ・撤退ラインはREADME記載の数値（8/31初報酬、9/30¥30,000、10/31¥100,000、未達時の対応）を
  そのまま踏襲し、勝手に目標を変更しない。目標変更はユーザーの明示的な指示があった場合のみ行う。
- 何か仕様に迷ったら、まず `affiliate-engine/README.md`（詳細仕様）→ `CLAUDE.md`（不変条件）→
  本ファイル（背景・理由）の順で参照し、それでも判断がつかない場合はユーザーに確認すること。

---

## 9. 引継ぎ手順（実行チェックリスト）

「ドキュメントを渡して終わり」ではなく、実際に保守主体を切り替えるための手順。
上から順に実行し、各ステップの完了条件を満たしてから次に進む。

### Step 1: アクセス権の付与（人間が行う。トークンの直渡しはしない）
- [ ] grokbuildの実行環境に、このリポジトリ（`fireworker011/research`）への読み書き権限を付与する
      （GitHub App/PAT等、grokbuild側の接続方式に従う。方式が不明な場合は本手順を進める前にユーザーに確認する）
- [ ] Threadsアクセストークン・Claude APIキー等の秘密情報は**GitHub Secretsに登録済みのものをそのまま使う**。
      チャットやファイルでの平文受け渡しは絶対にしない（§5参照）。grokbuildが自前でSecretsへ書き込む必要がある場合
      （例: `GH_SECRETS_PAT` 相当の権限）も、既存の「Secrets: Read and write」権限のFine-grained PATの発行手順
      （README「セットアップ」節）に従い、ユーザー自身がGitHub上で発行・登録する
- [ ] `affiliate_engine_post.yml` 等のワークフローが参照する Secrets 名一覧（`THREADS_<ACCOUNT>_USER_ID` /
      `_ACCESS_TOKEN` 等）を、値を見ずに**名前だけ** grokbuildに共有する（`config/accounts.json` の `env` フィールドを見れば列挙できる）

### Step 2: ドキュメントの引き渡し（読む順序を指定して渡す）
1. 本ファイル（`GROKBUILD_HANDOFF.md`）— 背景・理由・事故事例
2. `affiliate-engine/README.md` — 仕様・アーキテクチャ・ロードマップ・数値目標
3. `CLAUDE.md` — 不変条件の要約チェックリスト
4. `affiliate-engine/docs/account-strategy.md`, `docs/account-setup-kit.md` — アカウント運用の詳細
- [ ] grokbuildに上記4点を読ませた上で、**「§1の不変条件6項目を自分の言葉で要約させる」**などの形で
      理解度を確認する（読ませただけで検証しないと、後で不変条件を壊すリスクが残る）

### Step 3: 並走期間（いきなり完全移管しない）
- [ ] 最低1週間、grokbuildに**提案・PR作成まで**を担当させ、実際のマージ・pushは人間（またはClaude）が
      レビューしてから行う。特に §1-2（日付決定論）・§1-3（重複ガード）・§1-4（検品順序）に触れる変更は
      重点的にレビューする
- [ ] 並走期間中に発生した指摘事項・修正はこのファイルに追記し、grokbuild固有の弱点（誤解しやすい箇所）を
      可視化しておく

### Step 4: 完全移管の判定基準
- [ ] 並走期間中、§1の不変条件を壊す提案がゼロ件であること
- [ ] `node --check` / `strategy-engine.js --from-file` の決定論チェック / `threads-poster.js --dry-run`
      という §4 の検証手順を、指示なしで自発的に実行できていること
- [ ] 秘密情報をログ・コミット・チャットに出力した事例がゼロ件であること
- 上記3点を満たしたら、grokbuildに push までの権限を与え、Claudeは保守セッションから外れてよい
      （ただし本ファイル・README・CLAUDE.mdは以後もgrokbuildが更新責任を持つ）

### Step 5: ロールバック手順（grokbuildの変更で問題が起きた場合）
- [ ] 本番反映は「作業ブランチへのpush」がそのままデプロイになる設計（§1-1）なので、問題のあるコミットは
      `git revert` で作業ブランチに戻す（`reset --hard`等の履歴書き換えはしない。自動コミットと競合するため）
- [ ] 投稿事故（誤投稿・重複投稿等）が発生した場合は、まず該当ワークフロー（`affiliate_engine_post.yml`等）を
      GitHub上で一時的に無効化してから原因調査する（止血を優先する）
- [ ] ロールバック後は原因と再発防止策を本ファイル §1 に追記する（同じ事故を繰り返さないため。§1-2の事例と同じ形式）
